<?php


return array(
    'zfctwig' => array(
        'extensions' => array(
            // an extension that uses no key
            'SarTwig',
            // an extension with a key so that you can remove it from another module
            //'session' => 'SarTwigExtensions'
        )
    )
);
